import Component from "../../components/ui/Animationprogress.tsx";
import { Props } from "../../components/ui/Animationprogress.tsx";

export default function Islands({ children, id }: Props) {
  return <Component children={children} id={id} />;
}
