import Component from "../../components/ui/AnimationHeader.tsx";
import type { ComponentChildren } from "preact";

export default function Islands({ children }: { children: ComponentChildren }) {
  return <Component children={children} />;
}
